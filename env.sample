# Copy this file to `.env` for local development and fill in values as needed.
# Do NOT commit your real `.env`.

# --- Application ---
APP_ENV=dev
APP_PORT=8000

# --- Database ---
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb
